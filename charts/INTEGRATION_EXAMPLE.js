/**
 * Compatibility entrypoint for the arrow signal integration.
 *
 * The original file was a commented example. The implementation now lives in
 * signalIntegration.js so the bot can import and use it directly.
 */

module.exports = require('./signalIntegration');
